#pragma once

//////////////////////////////////////////////////////////////////
// Headers
//////////////////////////////////////////////////////////////////
#include <random>

//////////////////////////////////////////////////////////////////
/// \brief This is an objet to have persistant (and recuersive) seed
//////////////////////////////////////////////////////////////////
class randObj {

public:

   int seed;
   std::random_device rd;
   std::minstd_rand gen;

   randObj(int _seed = -1);
   

   float fRand(float min, float max);
   int iRand(int min, int max);

};
//////////////////////////////////////////////////////////////////
/// \brief Gets a random integer between the 2 given integers
//////////////////////////////////////////////////////////////////
int iRand(int min, int max);

//////////////////////////////////////////////////////////////////
/// \brief Gets a random integer between the 2 given integers
//////////////////////////////////////////////////////////////////
float fRand(float min, float max);
